import MainProducts from "@/components/MainProducts";

const page = () => {
  return (
    <main className="min-h-screen bg-[#fafafa] text-zinc-900">
      <MainProducts />
    </main>
  );
};

export default page;
